name: byter
description: Efficiently and Easy handle fixed sized byte data for example, unsigned 8 byte integers types.
version: 0.0.3
homepage: https://gitee.com/almpazel/dart-byter.git

## pub publish --server=https://pub.dartlang.org -v

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  convert: ^2.1.1
  logger: ^0.9.2

dev_dependencies:
  test: ^1.14.4
