name: pdfio
description: A dart lib for low-level PDF parser, parse objects, fonts, images and others.
version: 0.0.1
homepage: https://gitee.com/almpazel/dart-pdfio.git

## pub publish --server=https://pub.dartlang.org -v

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  convert: ^2.1.1
  logger: ^0.9.2

dev_dependencies:
  test: ^1.14.4
